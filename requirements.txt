# Python 3.10 or 3.11 required (pyBKT compatibility)
# Use: python -m venv .venv  then  pip install -r requirements.txt

# --- Backend API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-dotenv>=1.0,<2.0
psycopg[binary]>=3.1,<4.0

# --- BKT / data ---
pandas>=2.0,<3.0
openpyxl>=3.1,<4.0
numpy>=1.26,<2.0
scikit-learn>=1.3,<1.6
pyBKT>=1.4,<2.0

# --- LLM + RAG ---
langchain-core>=1.0,<2.0
langchain-groq>=1.0,<2.0
langchain-community>=0.3,<1.0
langchain-text-splitters>=0.3,<2.0
chromadb>=0.5,<2.0
pypdf>=4.0,<7.0
# Install CPU torch first in Dockerfile (see FastAPI-Backend/Dockerfile) so pip
# does not pull CUDA wheels that blow EC2 disk during image extract.
sentence-transformers>=3.0,<6.0

# --- Streamlit UIs ---
streamlit>=1.32,<2.0
requests>=2.31,<3.0
plotly>=5.0,<7.0